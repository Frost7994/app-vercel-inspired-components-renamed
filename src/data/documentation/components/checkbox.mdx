---
title: "Checkbox"
date: Oct 2022
author: Tommy Byrne
tag: "Components"
---

<GithubButton repo="/components/ui/checkbox.js" />

## Introduction

The `Checkbox` component is a controlled UI element used to toggle between two options, checked and unchecked. It can be styled to fit the design of your application, and supports various props to control its behavior and appearance.

## Anatomy

The `Checkbox` component consists of the following elements:

- `Checkbox` root as a wrapper 
- `Checkbox` indicator to provide visual feedback on its state  
- A simple check icon

## Usage

To use the `Checkbox` component, simply import it from `@/components/ui/checkbox` and use it as follows:

<CodeBlock
  title="Basic import"
  fit={true}
  snippet={`
    // state
    import { useState } from 'react'

    // components
    import Checkbox from '@/components/ui/checkbox'

    const Page = () => {
        // state
        let [checked, setChecked] = useState(false)

        return (
            <div className='flex items-center gap-2'>
                <Checkbox
                    checked={checked}
                    setChecked={setChecked}
                />
                <p className='text-lg'>Agree to terms and services?</p>
            </div>
        )
    }

    export default Page
    `}

/>

## Styling

The styling below is taken as an inspiration from the **Vercel** design principles. It allows for both light and dark mode and takes the following props:

- variant
- size
- disabled

You can use these variants and styles directly, edit the styling or edit the variants as you see fit for your project.

<CodeBlock
  title='General styling'
  snippet={`
   // styling
  let baseCheckboxStyles = cva(
    [
      "border",
      "overflow-hidden",
      "rounded-md",
      "flex",
      "items-center",
      "justify-center",
      "transition-all",
    ],
    {
      variants: {
        size: {
          sm: ["h-4", "w-4"],
          default: ["h-5", "w-5"],
          lg: ["h-6", "w-6"],
        },
        variant: {
          default: [
            "border-tertiary-300",
            "dark:border-tertiary-700",
            "hover:border-tertiary-950",
            "hover:dark:border-tertiary-50",
          ],
          primary: ["border-primary-500", "hover:border-primary-600"],
          secondary: ["border-secondary-500", "hover:border-secondary-600"],
        },
        disabled: {
          true: ["disabled:pointer-events-none", "disabled:opacity-40"],
        },
      },
      defaultVariants: {
        size: "default",
        variant: "default",
      },
    }
  );

  let baseCheckboxIndicatorStyles = cva(
    ["flex", "items-center", "justify-center", "h-full", "w-full"],
    {
      variants: {
        size: {
          sm: ["text-xs"],
          default: ["text-base"],
          lg: ["text-lg"],
        },
        variant: {
          default: [
            "bg-tertiary-950",
            "dark:bg-tertiary-50",
            "text-tertiary-50",
            "dark:text-tertiary-950",
          ],
          primary: ["bg-primary-500", "text-primary-50"],
          secondary: ["bg-secondary-500", "text-secondary-50"],
        },
      },
      defaultVariants: {
        variant: "default",
        size: "default",
      },
    }
  )
`
}
/>

## Functionality

The `Checkbox` is built using **@RadixUI**, **Class-Variance-Authority** and **TailwindCSS**. It is a simple UI component used to trigger events.

## Full Code

Below is the full code for `@/components/ui/checkbox`.

<CodeBlock 
title='Completed component'
snippet={`
// base
import { BiCheck } from "react-icons/bi";

// components
import * as CheckboxRoot from "@radix-ui/react-checkbox";

// utils
import { cva } from "class-variance-authority";
import mergeClasses from "@/utils/helpers/mergeClasses";

const Checkbox = ({
  disabled,
  checked,
  setChecked,
  defaultChecked = false,
  size,
  variant,
  className,
}) => {
  // styling
  let baseCheckboxStyles = cva(
    [
      "border",
      "overflow-hidden",
      "rounded-md",
      "flex",
      "items-center",
      "justify-center",
      "transition-all",
    ],
    {
      variants: {
        size: {
          sm: ["h-4", "w-4"],
          default: ["h-5", "w-5"],
          lg: ["h-6", "w-6"],
        },
        variant: {
          default: [
            "border-tertiary-300",
            "dark:border-tertiary-700",
            "hover:border-tertiary-950",
            "hover:dark:border-tertiary-50",
          ],
          primary: ["border-primary-500", "hover:border-primary-600"],
          secondary: ["border-secondary-500", "hover:border-secondary-600"],
        },
        disabled: {
          true: ["disabled:pointer-events-none", "disabled:opacity-40"],
        },
      },
      defaultVariants: {
        size: "default",
        variant: "default",
      },
    }
  );

  let baseCheckboxIndicatorStyles = cva(
    ["flex", "items-center", "justify-center", "h-full", "w-full"],
    {
      variants: {
        size: {
          sm: ["text-xs"],
          default: ["text-base"],
          lg: ["text-lg"],
        },
        variant: {
          default: [
            "bg-tertiary-950",
            "dark:bg-tertiary-50",
            "text-tertiary-50",
            "dark:text-tertiary-950",
          ],
          primary: ["bg-primary-500", "text-primary-50"],
          secondary: ["bg-secondary-500", "text-secondary-50"],
        },
      },
      defaultVariants: {
        variant: "default",
        size: "default",
      },
    }
  );

  return (
    <CheckboxRoot.Root
      disabled={disabled}
      id="c1"
      defaultChecked={defaultChecked}
      checked={checked}
      onCheckedChange={setChecked}
      className={mergeClasses(
        baseCheckboxStyles({ variant, size, disabled }),
        className
      )}
    >
      <CheckboxRoot.Indicator
        className={mergeClasses(baseCheckboxIndicatorStyles({ variant, size }))}
      >
        <BiCheck />
      </CheckboxRoot.Indicator>
    </CheckboxRoot.Root>
  );
};

export default Checkbox;


`
}/>

## Props

Below is a table of props for use within the `Checkbox` component.

| Prop                                          | Type                            | Default                         | Description                                                                                                                                                                                            |
| --------------------------------------------- | ------------------------------- | ------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| <TableProp type='solid'>defaultChecked</TableProp>      | <TableProp>Bool</TableProp>   | <NoWrap>false</NoWrap>      | The default state of the checkbox. |
| <TableProp type='solid'>checked</TableProp>      | <TableProp>Bool</TableProp>   | <NoWrap>false</NoWrap>      | The active state of the checkbox. |
| <TableProp type='solid'>setChecked</TableProp>      | <TableProp>Function</TableProp>   | <NoWrap>{`( ) => { }`}</NoWrap>      | Function to change the active state of the checkbox. |
| <TableProp type='solid'>size</TableProp>      | <TableProp>String</TableProp>   | <NoWrap>"default"</NoWrap>      | The size of the button. Possible values are `sm` , `default` and `lg` .                                                                                                                                |
| <TableProp type='solid'>variant</TableProp>   | <TableProp>String</TableProp>   | <NoWrap>"default"</NoWrap>      | The visual colors of the button. Possible values are `default` , `primary` and `secondary` .                                                                                                           |
| <TableProp type='solid'>disabled</TableProp>  | <TableProp>Bool</TableProp>     | <NoWrap>false</NoWrap>          | Disables the button.                                                                                                                                                                                   |
| <TableProp type='solid'>className</TableProp> | <TableProp>String</TableProp>   | <NoWrap>" "</NoWrap>            | CSS class name(s) to apply to the button element.                                                                                                                                                      |

## Demonstration

`Checkbox` has three variant options: __Default__, __Primary__ and __Secondary__. As well as that it can be checked, take a size prop and styling depending on it's disabled state.

<CheckboxDemonstration />
